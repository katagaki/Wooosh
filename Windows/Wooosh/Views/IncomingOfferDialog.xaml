<ContentDialog
    x:Class="Wooosh.Views.IncomingOfferDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:loc="using:Wooosh.Localization"
    Title="{loc:Localize Key=OfferNavIncoming}"
    CloseButtonText="{loc:Localize Key=ActionDecline}"
    DefaultButton="None"
    PrimaryButtonClick="OnPrimaryButtonClick"
    mc:Ignorable="d">

    <!-- DefaultButton is None: a security confirmation takes a deliberate press and is never bound to Enter. -->

    <StackPanel Spacing="12" MaxWidth="440">
        <TextBlock
            x:Name="OfferHeadline"
            Style="{ThemeResource BodyStrongTextBlockStyle}"
            TextWrapping="Wrap" />

        <TextBlock
            x:Name="OfferSummary"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{ThemeResource BodyTextBlockStyle}"
            TextWrapping="Wrap" />

        <TextBlock
            x:Name="PairedLabel"
            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
            Style="{ThemeResource BodyTextBlockStyle}"
            Text="{loc:Localize Key=OfferPairedDevice}"
            Visibility="Collapsed" />

        <StackPanel x:Name="UnpairedBlock" Spacing="8" Visibility="Collapsed">
            <TextBlock
                Style="{ThemeResource BodyTextBlockStyle}"
                Text="{loc:Localize Key=OfferUnpairedWarning}"
                TextWrapping="Wrap" />
            <!-- Never translated: the phrase must read identically on both devices. -->
            <TextBlock
                x:Name="FingerprintText"
                IsTextSelectionEnabled="True"
                Style="{ThemeResource BodyStrongTextBlockStyle}"
                TextWrapping="Wrap" />
        </StackPanel>
    </StackPanel>
</ContentDialog>
